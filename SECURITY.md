# Security policy

Do not report secrets in public issues. This project should never require a user's account password, application token, private signing key, or original proprietary APK in a public issue.

For compatibility reports, provide Android version, ROM build, target application version, `cmd overlay list` output, and the generated overlay status log after removing personal paths or identifiers.
